############################################
# WARNING:
# Managed by puppet - DO NOT modify locally.
############################################

# Change to 0 to disable daemon
ENABLE_DAEMON=1

# This directory stores some runtime information, like torrent files and links to the config file, which itself can be found in /etc/transmission-daemon/settings.json
CONFIG_DIR="<%= scope['::transmission::params::home_dir'] %>"

# Default options for daemon, see transmission-daemon(1) for more options
OPTIONS="--config-dir $CONFIG_DIR<% if scope['::transmission::log_file'] -%> --logfile <%= scope['::transmission::log_file'] %><% end -%>"
